export class Menu {
    url: string;
    title: string;
    icon: string;
  
    constructor(url: string, title: string, icon: string) {
        this.url = url;
        this.title = title;
        this.icon = icon;
    }

    
  }
  